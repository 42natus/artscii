# To-do for ascii-art-justify

1. order options around and observe output
2. display proper usage message for each case of invalid option usage
3. only output if filename follows <filename>.txt
4. ~~properly render input with multiple "\n"s in between text~~
5. account for missing space after "\n" for prompts like:  
   `go run . --align=justify "hello there! \nit's me" shadow`
